# Test environment variables
# Replace these with real test credentials or use GitHub secrets in CI/CD
ANTHROPIC_API_KEY="your anthropic_api_key"
SENDER_EMAIL="sender email"
SENDER_PASSWORD='sender app password'
TEST_EMAIL="test receiver email'"

# Set to "true" to skip tests that make external API calls
# This is useful for CI/CD pipelines where you might not want to make external calls
SKIP_EXTERNAL_CALLS=false
