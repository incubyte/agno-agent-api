# filepath: d:\development\client\agno-agent-api\pytest.ini
[pytest]
markers =
    e2e: marks tests as end-to-end tests
    integration: marks tests as integration tests (real API, mocked email)
python_paths = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
